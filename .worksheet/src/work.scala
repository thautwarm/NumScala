import scala.language.implicitConversions
import util.config._
import scala.compat.Platform.currentTime
import util.baseCalc.splitAt
object work{;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(170); 
		 
		 val a=List(1,2,3);System.out.println("""a  : List[Int] = """ + $show(a ));$skip(18); 
		 
		val b:Int=1;System.out.println("""b  : Int = """ + $show(b ));$skip(28); 
		
		b then ((x:Int)=>x+1)}
		
		

		
		
		
         

}
